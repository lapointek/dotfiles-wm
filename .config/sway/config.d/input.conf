# Mouse settings
input type:pointer {
    natural_scroll disabled
    accel_profile "flat"
    pointer_accel 1
}

# Touchpad settings
input type:touchpad {
    accel_profile "adaptive"
    pointer_accel 0.2
    tap enabled
    tap_button_map lrm
    click_method clickfinger
}
