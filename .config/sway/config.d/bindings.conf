#################
#   Variables   #
#################
set $mod Mod4
set $term foot
set $launcher rofi

# Power menu script
set $powermenu ~/.config/sway/scripts/power_menu.sh

# Vim keybindings
set $left h
set $down j
set $up k
set $right l

#####################
#  Basic Operations #
#####################
bindsym $mod+q kill
bindsym $mod+Shift+c reload

#######################
#  Floating Modifier  #
#######################
# Grab floating window
floating_modifier $mod normal

# Open power menu
bindsym $mod+Escape exec $powermenu

# Exit sway (logs you out of your Wayland session)
bindsym $mod+Shift+e exec swaynag -t warning -m 'You pressed the exit shortcut. Do you really want to exit sway? This will end your Wayland session.' -B 'Yes, exit sway' 'swaymsg exit'

# Start Swaylock
bindsym Ctrl+Alt+l exec swaylock -C ~/.config/sway/swaylock/config

##################
#  Focus Change  #
##################
# Move your focus around
bindsym $mod+$left focus left
bindsym $mod+$down focus down
bindsym $mod+$up focus up
bindsym $mod+$right focus right

# Move the focused window with the same, but add Shift
bindsym $mod+Shift+$left move left
bindsym $mod+Shift+$down move down
bindsym $mod+Shift+$up move up
bindsym $mod+Shift+$right move right

#######################
#  Switch Workspaces  #
#######################
bindsym $mod+1 workspace number 1
bindsym $mod+2 workspace number 2
bindsym $mod+3 workspace number 3
bindsym $mod+4 workspace number 4
bindsym $mod+5 workspace number 5
bindsym $mod+6 workspace number 6
bindsym $mod+7 workspace number 7
bindsym $mod+8 workspace number 8
bindsym $mod+9 workspace number 9
bindsym $mod+0 workspace number 10

# Move focused container to workspace
bindsym $mod+Shift+1 move container to workspace number 1
bindsym $mod+Shift+2 move container to workspace number 2
bindsym $mod+Shift+3 move container to workspace number 3
bindsym $mod+Shift+4 move container to workspace number 4
bindsym $mod+Shift+5 move container to workspace number 5
bindsym $mod+Shift+6 move container to workspace number 6
bindsym $mod+Shift+7 move container to workspace number 7
bindsym $mod+Shift+8 move container to workspace number 8
bindsym $mod+Shift+9 move container to workspace number 9
bindsym $mod+Shift+0 move container to workspace number 10

# Switch workspaces back and forth
bindsym $mod+Tab workspace back_and_forth

# Switch windows
bindsym alt+Tab focus right
bindsym alt+Shift+Tab focus left

# Resize window with right click
bindsym --whole-window {
    $mod+Shift+button4 resize grow width 5px, resize grow height 5px
    $mod+Shift+button5 resize shrink width 5px, resize shrink height 5px
}

######################
#  Window Modifying  #
######################
# Split window horizontally
bindsym $mod+c splith

# Split window vertically
bindsym $mod+v splitv

# Switch the current container between different layout styles
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# Make the current focus fullscreen
bindsym $mod+f fullscreen

# Toggle the current focus between tiling and floating mode
bindsym $mod+space floating toggle

# Swap focus between the tiling area and the floating area
bindsym $mod+Shift+space focus mode_toggle

# Move focus to the parent container
bindsym $mod+a focus parent

#####################
#  Window Resizing  #
#####################
# Resize window with vim keys
bindsym {
$mod+ctrl+$left resize grow width 20px
$mod+ctrl+$down resize shrink height 20px
$mod+ctrl+$up resize grow height 20px
$mod+ctrl+$right resize shrink width 20px
}

#######################################
#  Application Shortcuts and Actions  #
#######################################
# Start a terminal
bindsym $mod+Return exec $term

# Start launcher
bindsym $mod+d exec $launcher -show drun

# Thunar
bindsym $mod+o exec thunar

# Brave browser
bindsym $mod+b exec brave-browser

# Opencode
bindsym $mod+Shift+o exec foot -e bash -ic opencode

# Clipboard manager
bindsym $mod+Ctrl+v exec cliphist list | $launcher -dmenu -p "Select an entry to copy it to your clipboard buffer: "| cliphist decode | wl-copy

# Delete entry from clipboard
bindsym $mod+Ctrl+x exec cliphist list | $launcher -dmenu -p "Select an entry to delete it from cliphist: "| cliphist delete

# Start window switcher script
bindsym $mod+Shift+a exec ~/.config/sway/scripts/window_switcher.sh

################
#  Scratchpad  #
################
# Move the currently focused window to the scratchpad
bindsym $mod+Shift+minus move scratchpad

# Show the next scratchpad window
bindsym $mod+minus scratchpad show, floating disable

################
#  Brightness  #
################
# Requires brightnessctl
set $brightness_notification_cmd  command -v notify-send >/dev/null && \
        VALUE=$(brightnessctl --percentage get) && \
        notify-send -e -h string:x-canonical-private-synchronous:brightness \
            -h "int:value:$VALUE" -t 800 "Brightness: ${VALUE}%"

bindsym XF86MonBrightnessDown exec \
        'STEP="$brightness_step" && brightnessctl -q set ${STEP:-5}%- && $brightness_notification_cmd'
bindsym XF86MonBrightnessUp exec \
        'STEP="$brightness_step" && brightnessctl -q set +${STEP:-5}% && $brightness_notification_cmd'

###########
#  Media  #
###########
# Requires playerctl
# Allow Play and Stop bindings even if the screen is locked
bindsym --locked {
    XF86AudioPlay       exec playerctl play-pause
    XF86AudioStop       exec playerctl stop
}

bindsym {
    XF86AudioForward    exec playerctl position +10
    XF86AudioNext       exec playerctl next
    XF86AudioPause      exec playerctl pause
    XF86AudioPrev       exec playerctl previous
    XF86AudioRewind     exec playerctl position -10
}

################
#  Screenshot  #
################
# Requires grimshot
bindsym {
    # Capture the currently active output
    Insert       exec grimshot --notify save output
    # Capture the currently active window
    Alt+Insert   exec grimshot --notify save active
    # Capture a window selected manually
    Shift+Insert exec grimshot --notify save window
    # Select and capture a custom rectangular area
    Ctrl+Insert  exec grimshot --notify save area
}

############
#  Volume  #
############
set $volume_helper_cmd $HOME/.config/sway/scripts/volume_helper.sh
# Allow volume controls even if the screen is locked
bindsym --locked {
    XF86AudioRaiseVolume exec \
        $volume_helper_cmd --limit "$volume_limit" --increase "$volume_step"
    XF86AudioLowerVolume exec \
        $volume_helper_cmd --limit "$volume_limit" --decrease "$volume_step"
    XF86AudioMute    exec pactl set-sink-mute @DEFAULT_SINK@ toggle && $volume_helper_cmd
    XF86AudioMicMute exec pactl set-source-mute @DEFAULT_SOURCE@ toggle
}
